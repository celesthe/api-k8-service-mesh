apiVersion: v1
kind: Service
metadata:
  name: api-service2-reto5
  namespace: reto5
  labels:
    app: api-service2-reto5
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: api-service2-reto5